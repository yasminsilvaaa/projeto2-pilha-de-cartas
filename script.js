const cards = []
let option = '0';
do (option !== '3') {
    alert(`a quantidade de cartas no baralho: ${cards.length}\n1. Para adicionar uma carta.\n2. puxar uma carta\n3. sair`);
    option = prompt('escolha uma opção entre 1-3:')
    if (option === '1')
        const cardName = prompt("Digite o nome da carta que deseja adicionar")
    if (cardName) {

        
    }

} 
